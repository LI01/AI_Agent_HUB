# clients/claude_code/

Module-import form of `clients/claude-code/`. Use `python -m clients.claude_code.llm_agent`. The hyphenated directory has the human-facing README and the legacy non-LLM client.
