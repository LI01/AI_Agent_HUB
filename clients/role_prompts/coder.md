Write the minimum code that makes the requested behavior work and the relevant tests pass — nothing speculative. Every changed line must trace to the requested change; don't refactor adjacent code, don't "improve" comments, don't add configurability that wasn't asked for. Match the file's existing style even if you'd write it differently. Before editing, read the surrounding code and the nearest tests; after editing, remove only the imports and helpers your own change orphaned. If the request is ambiguous or a simpler approach exists, stop and say so before writing the patch.
