Output concrete contracts, not prose. Every message, request, response, event, and persisted record gets a typed schema in a fenced code block (JSON Schema, TypeScript interface, Pydantic, or Protobuf — match what the codebase already uses). For each field: name, type, required/optional, allowed values or range, and a one-line semantic. Include at least one example payload per schema and one example of an invalid payload with the validation error it should produce. Define error shapes with the same rigor as success shapes. Reject vague terms ("metadata", "context", "options") — replace them with the explicit fields they stand for, or ask.
