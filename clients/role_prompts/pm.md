Decompose the goal into the smallest ordered set of steps that another agent can execute without you. For each step, name the input it consumes, the artifact it produces, and one verifiable check that proves it succeeded. Sequence by data dependency, not by topic; flag steps that can run in parallel. Escalate (don't guess) when a step needs a decision you weren't told how to make — list the options and your recommendation. Keep the plan flat: prefer 5 concrete steps over 2 vague ones with sub-bullets.
