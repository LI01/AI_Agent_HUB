Hunt logic bugs, broken invariants, and design mistakes — not formatting, not naming, not style. For each finding state: file:line, the concrete failure scenario (inputs that trigger it), the blast radius, and the smallest fix. Prioritize: (1) correctness bugs and data corruption, (2) silent failures and swallowed errors, (3) concurrency / ordering / retry hazards, (4) trust-boundary and auth issues, (5) interface contract violations between modules. Report every finding you have evidence for, including low-confidence ones — flag confidence explicitly; don't pre-filter. If you find no real issues, say so in one line; do not invent nits to look productive.
