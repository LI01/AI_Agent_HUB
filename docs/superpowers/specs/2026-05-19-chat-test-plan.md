# Agent Hub Chat — 测试计划

**日期:** 2026-05-19
**环境:** https://agent-hub.muffinfluffy.com
**测试账号:**
- Admin: `leonl@leopardimaging.com`
- 普通用户: `leonl@aglaiasense.com`

---

## 1. 登录与路由

| # | 测试项 | 步骤 | 预期结果 | 状态 |
|---|--------|------|---------|------|
| 1.1 | 根路径重定向 | 访问 `https://agent-hub.muffinfluffy.com/` | 自动跳转到 `/chat` | ☐ |
| 1.2 | CF Access 登录 | 访问 `/chat`，未登录时 | 弹出 CF Access 登录页面 | ☐ |
| 1.3 | 登录后进入聊天页 | 用 `leonl@aglaiasense.com` 登录 | 看到聊天界面，无"未登录"弹窗 | ☐ |
| 1.4 | JWT 获取 | 登录后打开浏览器 DevTools → Network | `/chat/jwt` 返回非空 token | ☐ |

## 2. 学员聊天功能（普通用户）

| # | 测试项 | 步骤 | 预期结果 | 状态 |
|---|--------|------|---------|------|
| 2.1 | 顶部栏显示 | 登录后查看 | 显示"嘉骏 AI 培训平台"、邮箱、WS 连接状态（绿色圆点） | ☐ |
| 2.2 | 创建对话 | 点击"+ 新对话" | 弹出 Agent 选择器，显示 available 的 agents | ☐ |
| 2.3 | 选择 Agent | 选择一个 agent | 创建对话，左侧列表出现新对话 | ☐ |
| 2.4 | 发送消息 | 输入文字，点击发送 | 消息出现在聊天区，Agent 显示"正在思考..." | ☐ |
| 2.5 | Agent 回复 | 等待 Agent 处理 | Agent 回复出现在聊天区，支持 Markdown 渲染 | ☐ |
| 2.6 | 对话历史 | 刷新页面 | 之前的对话保留在左侧列表 | ☐ |
| 2.7 | 切换对话 | 点击左侧不同对话 | 聊天区切换到对应对话的历史消息 | ☐ |
| 2.8 | 工作区文件浏览 | 查看左侧底部"📂 工作区" | 显示用户工作区文件列表 | ☐ |
| 2.9 | 提交作业 | 点击"提交作业"按钮 | 弹出确认，按钮变为"✓ 已提交" | ☐ |
| 2.10 | 提交后只读 | 提交后尝试发送消息 | 显示错误"conversation is submitted and read-only" | ☐ |

## 3. Admin 管理功能

| # | 测试项 | 步骤 | 预期结果 | 状态 |
|---|--------|------|---------|------|
| 3.1 | 访问管理页 | 用 `leonl@leopardimaging.com` 登录，访问 `/chat/admin` | 看到管理界面，4 个 Tab | ☐ |
| 3.2 | 看板 Tab | 点击"📊 看板" | 显示总学员数、对话数、提交数，各小组进度条 | ☐ |
| 3.3 | 学员 Tab | 点击"👥 学员" | 显示用户列表（邮箱、姓名、角色、最后活跃） | ☐ |
| 3.4 | 学员搜索 | 在搜索框输入邮箱 | 过滤显示匹配的用户 | ☐ |
| 3.5 | Agents Tab | 点击"🤖 Agents" | 显示所有 agent 列表，状态标签（idle/busy/offline） | ☐ |
| 3.6 | 新建 Agent | 点击"+ 新建 Agent"，填写表单并提交 | 生成 API Key（显示一次），Agent 出现在列表 | ☐ |
| 3.7 | 作业 Tab | 点击"📝 作业" | 显示所有提交记录 | ☐ |
| 3.8 | 筛选作业 | 点击"待评分"/"已评分" | 过滤显示对应状态的作业 | ☐ |
| 3.9 | 评分作业 | 点击"评分"，输入分数和反馈 | 提交成功，作业状态变为"reviewed" | ☐ |
| 3.10 | 返回聊天 | 点击"← 返回聊天" | 跳转到 `/chat` 页面 | ☐ |

## 4. 安全

| # | 测试项 | 步骤 | 预期结果 | 状态 |
|---|--------|------|---------|------|
| 4.1 | 未认证访问 API | 用 curl 访问 `/chat/conversations`（无 JWT） | 返回 403 | ☐ |
| 4.2 | 未认证访问 Admin | 用普通用户访问 `/chat/admin` | 返回 403 或无权访问 | ☐ |
| 4.3 | 用户数据隔离 | 普通用户 A 看不到用户 B 的对话 | 只能看到自己的对话 | ☐ |
| 4.4 | 文件路径遍历 | 尝试访问 `../` 路径的文件 | 返回 400 错误 | ☐ |
| 4.5 | 文件上传大小限制 | 上传超过 10 MiB 的文件 | 返回 413 错误 | ☐ |

## 5. WebSocket 实时通信

| # | 测试项 | 步骤 | 预期结果 | 状态 |
|---|--------|------|---------|------|
| 5.1 | WS 连接 | 登录后打开 DevTools → Network → WS | 看到 `/chat/ws` 连接成功 | ☐ |
| 5.2 | 实时消息 | 发送消息 | 无需刷新即可看到 Agent 回复 | ☐ |
| 5.3 | 断线重连 | 关闭网络再恢复 | WS 自动重连（绿色圆点恢复） | ☐ |

---

## 测试执行记录

| 测试人 | 日期 | 通过 | 失败 | 备注 |
|--------|------|------|------|------|
| | | | | |
