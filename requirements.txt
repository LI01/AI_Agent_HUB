# Runtime dependencies for the hub, SDK, and MCP server.
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
sse-starlette>=1.6.0
httpx>=0.27.0
websockets>=12.0.0
websocket-client>=1.6.0

# Test dependencies (used by tests/ and scripts/e2e/)
pytest>=8.0.0
pytest-asyncio>=0.23.0
